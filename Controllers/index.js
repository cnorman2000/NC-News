const { } = require('./api.controller');
const { } = require('./articles..controller');
const { } = require('comments.controller');
const { } = require('topics.controller');
const { } = require('users.controller');

module.exports = {
  
}